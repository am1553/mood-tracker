# Mood Tracker by FrontendMentor

## Setting Up Environment

Run the following code in the terminal to set up the right node version and starting the application.

```
nvm use
npm install
npm run dev
```
